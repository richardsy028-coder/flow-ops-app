Create a complete Node.js + Express web app called Clarity Systems / FLOW AI Operations Companion.

Requirements:
1. Use Express backend.
2. Serve frontend from /public/index.html.
3. Add homepage route so "/" loads index.html.
4. Add POST /api/diagnose endpoint.
5. Use OpenAI API from .env only.
6. Never expose API key in frontend.
7. Use PORT from .env, default 3008.
8. Return JSON diagnosis from AI.
9. Frontend should have:
   - premium dark SaaS design
   - hero section
   - FLOW framework section
   - industries section
   - AI diagnosis form
   - founder section for Richard Sy
   - pricing section
   - CTA with email richardsy.028@gmail.com
10. Include files:
   - server.js
   - package.json
   - .env.example
   - public/index.html
11. Make sure app runs with:
   npm install
   node server.js
12. App should open at:
   http://localhost:3008

Fix common issues:
- include app.get("/")
- include app.use(express.static(path.join(__dirname, "public")))
- include require("dotenv").config()
- include const app = express()
- include app.listen(PORT)